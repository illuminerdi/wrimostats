<fieldset>
  <legend>Editing Word War: <%= @word_war.title %></legend>
  <%= render :partial => 'form', :locals => {:form_type => "Update"}%>
</fieldset>
<%= link_to 'Back', word_war_path(@word_war) %>